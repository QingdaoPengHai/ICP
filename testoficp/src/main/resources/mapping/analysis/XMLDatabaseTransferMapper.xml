<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.penghai.testoficp.analysis.dao.mybatis.XMLDatabaseTransferDaoI" >
  
  <!-- 执行任意sql -->
  <select id="executeSQL" parameterType="string" resultType="java.util.LinkedHashMap">
  	${value}
  </select>
  
  <!-- 获取一个测试JSON字符串__正式发布需删除 -->
  <select id="getJson" resultType="string">
  	select name from test.name where id = 1
  </select>
</mapper>